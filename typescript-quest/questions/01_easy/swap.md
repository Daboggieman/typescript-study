# Swap

Source: swap

Files to submit
- solution.ts

Allowed functions
- --allow-builtin

Instructions
Write a function that swaps two values and returns them as a list.

Expected function
```ts
function swap(a: number, b: number): number[] {
  return [];
}
```


Here is a possible program to test your function :
```ts
const [x, y] = swap(1, 2);
console.log(x, y);
```

And its output :
```text
2 1
```
