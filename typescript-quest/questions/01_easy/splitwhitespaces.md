# Splitwhitespaces

Source: splitwhitespaces

Files to submit
- solution.ts

Allowed functions
- --allow-builtin

Instructions
Write a function that splits a string into a list of words, using whitespace as the delimiter.

Expected function
```ts
function split_whitespace(s: string): string[] {
  return [];
}
```


Here is a possible program to test your function :
```ts
console.log(split_whitespace("hello world"));
```

And its output :
```text
[ 'hello', 'world' ]
```
