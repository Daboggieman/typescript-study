# Print_words_tables

Source: print_words_tables

Files to submit
- solution.ts

Allowed functions
- --allow-builtin

Instructions
Write a function that prints a list of words in a table format.

Expected function
```ts
function print_words_tables(words: string[]): void {
  return;
}
```


Here is a possible program to test your function :
```ts
print_words_tables(["one", "two"]);
```

And its output :
```text
one
two
```
