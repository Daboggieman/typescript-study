# Recursive_power

Source: recursive_power

Files to submit
- solution.ts

Allowed functions
- --allow-builtin

Instructions
Write a recursive function that computes base raised to exp.

Expected function
```ts
function recursive_power(base: number, exp: number): number {
  return 0;
}
```


Here is a possible program to test your function :
```ts
console.log(recursive_power(2, 4));
```

And its output :
```text
16
```
