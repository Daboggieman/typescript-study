# Iterative_power

Source: iterative_power

Files to submit
- solution.ts

Allowed functions
- --allow-builtin

Instructions
Write a function that computes base raised to exp using iteration.

Expected function
```ts
function iterative_power(base: number, exp: number): number {
  return 0;
}
```


Here is a possible program to test your function :
```ts
console.log(iterative_power(2, 4));
```

And its output :
```text
16
```
